# rating_NN.py

# Mock function for movie_rater
# Replace this with your actual model and logic
def movie_rater(features):
    """
    Takes a list of movie features and returns a predicted rating.
    Example of features: [title, director, cast, description, duration]
    """
    # Replace this with your model's prediction logic
    print(f"Received features: {features}")
    return 8.7  # Dummy rating
